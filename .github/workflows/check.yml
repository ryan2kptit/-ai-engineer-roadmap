name: check
on: [push, pull_request]
jobs:
  lint-notes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - run: node scripts/scaffold.mjs
      - run: node scripts/progress.mjs --check
